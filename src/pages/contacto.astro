---
import Layout from "@/layouts/Layout.astro"
---

<Layout>
  <main class="pt-20 min-h-screen bg-black text-white">
    <section class="container mx-auto px-6 py-20">
      <h1 class="text-5xl font-bold mb-8 text-center">Contacto</h1>
      <div class="max-w-2xl mx-auto">
        <form class="space-y-6 bg-gray-900 p-8 rounded-lg">
          <div>
            <label for="nombre" class="block mb-2 text-sm font-medium">
              Nombre
            </label>
            <input
              type="text"
              id="nombre"
              name="nombre"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white focus:outline-none focus:border-[#AF49FF]"
              required
            />
          </div>
          <div>
            <label for="email" class="block mb-2 text-sm font-medium">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white focus:outline-none focus:border-[#AF49FF]"
              required
            />
          </div>
          <div>
            <label for="mensaje" class="block mb-2 text-sm font-medium">
              Mensaje
            </label>
            <textarea
              id="mensaje"
              name="mensaje"
              rows="6"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white focus:outline-none focus:border-[#AF49FF]"
              required
            ></textarea>
          </div>
          <button
            type="submit"
            class="w-full bg-[#AF49FF] text-white font-semibold py-3 px-6 rounded-lg hover:bg-[#9d3fe6] transition-colors"
          >
            Enviar Mensaje
          </button>
        </form>
      </div>
    </section>
  </main>
</Layout>

